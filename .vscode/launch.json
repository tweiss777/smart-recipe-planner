{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "expo",
            "request": "attach",
            "name": "Inspect Expo app",
            "projectRoot": "${workspaceFolder}",
            "bundlerHost": "127.0.0.1"
        }
    ]
}